import 'package:flutter/material.dart';

class Result extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'No more questions are present !!',
        style: TextStyle(
          fontSize: 24,
        ),
      ),
    );
  }
}
